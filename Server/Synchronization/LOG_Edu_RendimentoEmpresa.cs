// ###################################################### 
// ## SyCraf Engine Codegen                          #### 
// ###################################################### 
// ## This file is entirely written by               #### 
// ## the construction bot. DO NOT EDIT THIS FILE.   #### 
// ###################################################### 

using System;
using System.IO;
using System.Threading;
using System.Collections;

namespace SyCrafEngine
{
	public class LOG_Edu_RendimentoEmpresa : Synchronization
	{
		private string i_unique = "0";
		private string fk_empresaVirtual = "0";
		private string vr_pct = "0";
		private string dt_rend = "1900-01-01 00:00:00";
		private string tg_neg = "";
		
		public int var_FieldCount = 5;
		
		public LOG_Edu_RendimentoEmpresa ()
		{
			var_Serial = TB_LOG_EDU_RENDIMENTOEMPRESA.serial;
			var_Tablename = TB_LOG_EDU_RENDIMENTOEMPRESA.Alias;
		}
		
		public LOG_Edu_RendimentoEmpresa ( Transaction trx )
		{
			AcquireTransaction ( ref trx );
			
			var_Serial = TB_LOG_EDU_RENDIMENTOEMPRESA.serial;
			var_Tablename = TB_LOG_EDU_RENDIMENTOEMPRESA.Alias;
			
			TraceLog ( var_Tablename + " serial : " + var_serialNumber);
		}
		
		public string exportNames()
		{
			return "fk_empresaVirtual,vr_pct,dt_rend,tg_neg";
		}
		
		public string exportCSV()
		{
			return i_unique + str_field_sep + fk_empresaVirtual + str_field_sep + vr_pct + str_field_sep + dt_rend + str_field_sep + tg_neg;
		}
		
		public void Reset()
		{
			i_unique = "0";
			fk_empresaVirtual = "0";
			vr_pct = "0";
			dt_rend = "1900-01-01 00:00:00";
			tg_neg = "";
		
			ReleaseExclusive();
		}
		
		public void copy ( ref LOG_Edu_RendimentoEmpresa cpy )
		{
			fk_empresaVirtual = cpy.fk_empresaVirtual;
			vr_pct = cpy.vr_pct;
			dt_rend = cpy.dt_rend;
			tg_neg = cpy.tg_neg;
		}
		
		public override void fetchRetrieve ( ref DB_Row row )
		{
			i_unique = row.GetField ( TB_LOG_EDU_RENDIMENTOEMPRESA.i_unique );
			fk_empresaVirtual = row.GetField ( TB_LOG_EDU_RENDIMENTOEMPRESA.fk_empresaVirtual );
			vr_pct = row.GetField ( TB_LOG_EDU_RENDIMENTOEMPRESA.vr_pct );
			dt_rend = ConvertTime ( row.GetField ( TB_LOG_EDU_RENDIMENTOEMPRESA.dt_rend ) );
			tg_neg = row.GetField ( TB_LOG_EDU_RENDIMENTOEMPRESA.tg_neg );
		}
		
		public bool synchronize_LOG_Edu_RendimentoEmpresa ( )
		{
			try{
			
			if ( m_hshChangedFields.Count == 0 )
				return true;
			
			StartUpdate();
		
			if ( m_hshChangedFields [ "fk_empresaVirtual" ] != null )
				m_gen_dbStatement.AddUpdate ( TB_LOG_EDU_RENDIMENTOEMPRESA.fk_empresaVirtual , fk_empresaVirtual , TB_LOG_EDU_RENDIMENTOEMPRESA.type_fk_empresaVirtual );
			if ( m_hshChangedFields [ "vr_pct" ] != null )
				m_gen_dbStatement.AddUpdate ( TB_LOG_EDU_RENDIMENTOEMPRESA.vr_pct , vr_pct , TB_LOG_EDU_RENDIMENTOEMPRESA.type_vr_pct );
			if ( m_hshChangedFields [ "dt_rend" ] != null )
				m_gen_dbStatement.AddUpdate ( TB_LOG_EDU_RENDIMENTOEMPRESA.dt_rend , dt_rend , TB_LOG_EDU_RENDIMENTOEMPRESA.type_dt_rend );
			if ( m_hshChangedFields [ "tg_neg" ] != null )
				m_gen_dbStatement.AddUpdate ( TB_LOG_EDU_RENDIMENTOEMPRESA.tg_neg , tg_neg , TB_LOG_EDU_RENDIMENTOEMPRESA.type_tg_neg );
		
			m_gen_dbStatement.AddWhere ( TB_LOG_EDU_RENDIMENTOEMPRESA.i_unique, i_unique, TB_LOG_EDU_RENDIMENTOEMPRESA.type_i_unique );
		
			} catch ( System.Exception ex ) {
				StreamWriter wr = new StreamWriter ( 	"CRITICAL_" + var_Tablename + "_" + "_" + DateTime.Now.Year.ToString() + DateTime.Now.Month.ToString().PadLeft ( 2, '0' ) + DateTime.Now.Day.ToString().PadLeft ( 2, '0' ) + "_" + DateTime.Now.Hour.ToString().PadLeft ( 2, '0' ) + DateTime.Now.Minute.ToString().PadLeft ( 2, '0' ) + DateTime.Now.Second.ToString().PadLeft ( 2, '0' ) + ".txt" );
				wr.WriteLine ( ex.ToString() );
				wr.Flush();
				wr.Close();
			}
		
			return Update();
		}
			
		public string get_identity() { return i_unique; } 
		public string get_fk_empresaVirtual() { return fk_empresaVirtual; } 
		public string get_vr_pct() { return vr_pct; } 
		public string get_dt_rend() { return dt_rend; } 
		public string get_tg_neg() { return tg_neg; } 
			
		public long get_int_fk_empresaVirtual() { return Convert.ToInt64 ( fk_empresaVirtual ); } 
		public long get_int_vr_pct() { return Convert.ToInt64 ( vr_pct ); } 
			
		public void set_fk_empresaVirtual ( string val ) { fk_empresaVirtual = val; m_hshChangedFields [ "fk_empresaVirtual" ]="."; } 
		public void set_vr_pct ( string val ) { vr_pct = val; m_hshChangedFields [ "vr_pct" ]="."; } 
		public void set_dt_rend ( string val ) { dt_rend = val; m_hshChangedFields [ "dt_rend" ]="."; } 
		public void set_tg_neg ( string val ) { tg_neg = val; m_hshChangedFields [ "tg_neg" ]="."; } 
		
		public void set_fk_empresaVirtual ( long val ) { fk_empresaVirtual = Convert.ToString(val); m_hshChangedFields [ "fk_empresaVirtual" ]="."; } 
		public void set_vr_pct ( long val ) { vr_pct = Convert.ToString(val); m_hshChangedFields [ "vr_pct" ]="."; } 
			
		public void fieldSelection()
		{
			m_gen_dbStatement.AddSelect ( TB_LOG_EDU_RENDIMENTOEMPRESA.i_unique );
			m_gen_dbStatement.AddSelect ( TB_LOG_EDU_RENDIMENTOEMPRESA.fk_empresaVirtual );
			m_gen_dbStatement.AddSelect ( TB_LOG_EDU_RENDIMENTOEMPRESA.vr_pct );
			m_gen_dbStatement.AddSelect ( TB_LOG_EDU_RENDIMENTOEMPRESA.dt_rend );
			m_gen_dbStatement.AddSelect ( TB_LOG_EDU_RENDIMENTOEMPRESA.tg_neg );
		}
			
		public bool selectAll()
		{
			do
			{
				StartSelect();
				fieldSelection();
				
				if ( !executeQuery() )
					return false;
			}
			while ( EndSelect() == false ); // row lock
			
			return  HasRows();
		}
		
		public bool selectIdentity ( string identity )
		{
			do
			{
				StartSelect();
				fieldSelection();
			
				m_gen_dbStatement.AddWhere ( TB_LOG_EDU_RENDIMENTOEMPRESA.i_unique, identity, TB_LOG_EDU_RENDIMENTOEMPRESA.type_i_unique );
			
				if ( !executeQuery())
					return false;
			}
			while ( EndSelect() == false ); // row lock
			
			if ( HasRows() == true)
			{
				DB_Row row = m_db_result.GetFirstRow();
				fetchRetrieve ( ref row );
				return true;
			}
			
			return false;
		}
		
		public bool create_LOG_Edu_RendimentoEmpresa ( )
		{
			try{
			string new_id = "";
			
			StartInsert();
			
			m_gen_dbStatement.AddValue ( TB_LOG_EDU_RENDIMENTOEMPRESA.fk_empresaVirtual, fk_empresaVirtual, TB_LOG_EDU_RENDIMENTOEMPRESA.type_fk_empresaVirtual );
			m_gen_dbStatement.AddValue ( TB_LOG_EDU_RENDIMENTOEMPRESA.vr_pct, vr_pct, TB_LOG_EDU_RENDIMENTOEMPRESA.type_vr_pct );
			m_gen_dbStatement.AddValue ( TB_LOG_EDU_RENDIMENTOEMPRESA.dt_rend, dt_rend, TB_LOG_EDU_RENDIMENTOEMPRESA.type_dt_rend );
			m_gen_dbStatement.AddValue ( TB_LOG_EDU_RENDIMENTOEMPRESA.tg_neg, tg_neg, TB_LOG_EDU_RENDIMENTOEMPRESA.type_tg_neg );
			
			if ( !ExecuteScalar ( ref new_id ) )
				return false;
			
			i_unique = new_id;
			
			} catch ( System.Exception ex ) {
				StreamWriter wr = new StreamWriter ( 	"CRITICAL_" + var_Tablename + "_" + "_" + DateTime.Now.Year.ToString() + DateTime.Now.Month.ToString().PadLeft ( 2, '0' ) + DateTime.Now.Day.ToString().PadLeft ( 2, '0' ) + "_" + DateTime.Now.Hour.ToString().PadLeft ( 2, '0' ) + DateTime.Now.Minute.ToString().PadLeft ( 2, '0' ) + DateTime.Now.Second.ToString().PadLeft ( 2, '0' ) + ".txt" );
				wr.WriteLine ( ex.ToString() );
				wr.Flush();
				wr.Close();
			}
			
			return true;
		}
			
		public bool delete ( )
		{
		
			bool ret = false;
		
			try{
			StartDelete();
			
			if ( i_unique != "0" )
				m_gen_dbStatement.AddWhere ( TB_LOG_EDU_RENDIMENTOEMPRESA.i_unique, i_unique, TB_LOG_EDU_RENDIMENTOEMPRESA.type_i_unique );
			
			ret = Execute();
			
			} catch ( System.Exception ex ) {
				StreamWriter wr = new StreamWriter ( 	"CRITICAL_" + var_Tablename + "_" + "_" + DateTime.Now.Year.ToString() + DateTime.Now.Month.ToString().PadLeft ( 2, '0' ) + DateTime.Now.Day.ToString().PadLeft ( 2, '0' ) + "_" + DateTime.Now.Hour.ToString().PadLeft ( 2, '0' ) + DateTime.Now.Minute.ToString().PadLeft ( 2, '0' ) + DateTime.Now.Second.ToString().PadLeft ( 2, '0' ) + ".txt" );
				wr.WriteLine ( ex.ToString() );
				wr.Flush();
				wr.Close();
			}
			
			return ret; 
		}
			
		public bool select_rows_date ( string val_dt_rend, string val_fk_empresaVirtual )
		{
			do
			{
				StartSelect();
				fieldSelection();
			
				m_gen_dbStatement.AddWhere ( TB_LOG_EDU_RENDIMENTOEMPRESA.dt_rend, val_dt_rend , TB_LOG_EDU_RENDIMENTOEMPRESA.type_dt_rend);
				m_gen_dbStatement.AddWhere ( TB_LOG_EDU_RENDIMENTOEMPRESA.fk_empresaVirtual, val_fk_empresaVirtual , TB_LOG_EDU_RENDIMENTOEMPRESA.type_fk_empresaVirtual);
			
				if ( !executeQuery() )
					return false;
			}
			while ( EndSelect() == false ); // row lock
			
			return HasRows();
		}
		
		#if DEBUG
		
		public bool setup ( bool IsTruncate, ref string id, string var_fk_empresaVirtual, string var_vr_pct, string var_dt_rend, string var_tg_neg )
		{
			if ( IsTruncate ) 
			{
				i_unique = "";
				this.delete();
			}
		
			set_fk_empresaVirtual ( var_fk_empresaVirtual );
			set_vr_pct ( var_vr_pct );
			set_dt_rend ( var_dt_rend );
			set_tg_neg ( var_tg_neg );
			
			if ( create_LOG_Edu_RendimentoEmpresa() == false )
			{
				return false;
			}
			
			id = get_identity();
			return true;
		}
		
		#endif
		
	}
}
