// ###################################################### 
// ## SyCraf Engine Codegen                          #### 
// ###################################################### 
// ## This file is entirely written by               #### 
// ## the construction bot. DO NOT EDIT THIS FILE.   #### 
// ###################################################### 

using System;
using System.IO;
using System.Threading;
using System.Collections;

namespace SyCrafEngine
{
	public class LINK_PFAtivacao : Synchronization
	{
		private string i_unique = "0";
		private string fk_payfone = "0";
		private string dt_ativacao = "1900-01-01 00:00:00";
		private string st_ativacao = "";
		private string tg_status = "";
		
		public int var_FieldCount = 5;
		
		public LINK_PFAtivacao ()
		{
			var_Serial = TB_LINK_PFATIVACAO.serial;
			var_Tablename = TB_LINK_PFATIVACAO.Alias;
		}
		
		public LINK_PFAtivacao ( Transaction trx )
		{
			AcquireTransaction ( ref trx );
			
			var_Serial = TB_LINK_PFATIVACAO.serial;
			var_Tablename = TB_LINK_PFATIVACAO.Alias;
			
			TraceLog ( var_Tablename + " serial : " + var_serialNumber);
		}
		
		public string exportNames()
		{
			return "fk_payfone,dt_ativacao,st_ativacao,tg_status";
		}
		
		public string exportCSV()
		{
			return i_unique + str_field_sep + fk_payfone + str_field_sep + dt_ativacao + str_field_sep + st_ativacao + str_field_sep + tg_status;
		}
		
		public void Reset()
		{
			i_unique = "0";
			fk_payfone = "0";
			dt_ativacao = "1900-01-01 00:00:00";
			st_ativacao = "";
			tg_status = "";
		
			ReleaseExclusive();
		}
		
		public void copy ( ref LINK_PFAtivacao cpy )
		{
			fk_payfone = cpy.fk_payfone;
			dt_ativacao = cpy.dt_ativacao;
			st_ativacao = cpy.st_ativacao;
			tg_status = cpy.tg_status;
		}
		
		public override void fetchRetrieve ( ref DB_Row row )
		{
			i_unique = row.GetField ( TB_LINK_PFATIVACAO.i_unique );
			fk_payfone = row.GetField ( TB_LINK_PFATIVACAO.fk_payfone );
			dt_ativacao = ConvertTime ( row.GetField ( TB_LINK_PFATIVACAO.dt_ativacao ) );
			st_ativacao = row.GetField ( TB_LINK_PFATIVACAO.st_ativacao );
			tg_status = row.GetField ( TB_LINK_PFATIVACAO.tg_status );
		}
		
		public bool synchronize_LINK_PFAtivacao ( )
		{
			try{
			
			if ( m_hshChangedFields.Count == 0 )
				return true;
			
			StartUpdate();
		
			if ( m_hshChangedFields [ "fk_payfone" ] != null )
				m_gen_dbStatement.AddUpdate ( TB_LINK_PFATIVACAO.fk_payfone , fk_payfone , TB_LINK_PFATIVACAO.type_fk_payfone );
			if ( m_hshChangedFields [ "dt_ativacao" ] != null )
				m_gen_dbStatement.AddUpdate ( TB_LINK_PFATIVACAO.dt_ativacao , dt_ativacao , TB_LINK_PFATIVACAO.type_dt_ativacao );
			if ( m_hshChangedFields [ "st_ativacao" ] != null )
				m_gen_dbStatement.AddUpdate ( TB_LINK_PFATIVACAO.st_ativacao , st_ativacao , TB_LINK_PFATIVACAO.type_st_ativacao );
			if ( m_hshChangedFields [ "tg_status" ] != null )
				m_gen_dbStatement.AddUpdate ( TB_LINK_PFATIVACAO.tg_status , tg_status , TB_LINK_PFATIVACAO.type_tg_status );
		
			m_gen_dbStatement.AddWhere ( TB_LINK_PFATIVACAO.i_unique, i_unique, TB_LINK_PFATIVACAO.type_i_unique );
		
			} catch ( System.Exception ex ) {
				StreamWriter wr = new StreamWriter ( 	"CRITICAL_" + var_Tablename + "_" + "_" + DateTime.Now.Year.ToString() + DateTime.Now.Month.ToString().PadLeft ( 2, '0' ) + DateTime.Now.Day.ToString().PadLeft ( 2, '0' ) + "_" + DateTime.Now.Hour.ToString().PadLeft ( 2, '0' ) + DateTime.Now.Minute.ToString().PadLeft ( 2, '0' ) + DateTime.Now.Second.ToString().PadLeft ( 2, '0' ) + ".txt" );
				wr.WriteLine ( ex.ToString() );
				wr.Flush();
				wr.Close();
			}
		
			return Update();
		}
			
		public string get_identity() { return i_unique; } 
		public string get_fk_payfone() { return fk_payfone; } 
		public string get_dt_ativacao() { return dt_ativacao; } 
		public string get_st_ativacao() { return st_ativacao; } 
		public string get_tg_status() { return tg_status; } 
			
		public long get_int_fk_payfone() { return Convert.ToInt64 ( fk_payfone ); } 
			
		public void set_fk_payfone ( string val ) { fk_payfone = val; m_hshChangedFields [ "fk_payfone" ]="."; } 
		public void set_dt_ativacao ( string val ) { dt_ativacao = val; m_hshChangedFields [ "dt_ativacao" ]="."; } 
		public void set_st_ativacao ( string val ) { st_ativacao = val; m_hshChangedFields [ "st_ativacao" ]="."; } 
		public void set_tg_status ( string val ) { tg_status = val; m_hshChangedFields [ "tg_status" ]="."; } 
		
		public void set_fk_payfone ( long val ) { fk_payfone = Convert.ToString(val); m_hshChangedFields [ "fk_payfone" ]="."; } 
			
		public void fieldSelection()
		{
			m_gen_dbStatement.AddSelect ( TB_LINK_PFATIVACAO.i_unique );
			m_gen_dbStatement.AddSelect ( TB_LINK_PFATIVACAO.fk_payfone );
			m_gen_dbStatement.AddSelect ( TB_LINK_PFATIVACAO.dt_ativacao );
			m_gen_dbStatement.AddSelect ( TB_LINK_PFATIVACAO.st_ativacao );
			m_gen_dbStatement.AddSelect ( TB_LINK_PFATIVACAO.tg_status );
		}
			
		public bool selectAll()
		{
			do
			{
				StartSelect();
				fieldSelection();
				
				if ( !executeQuery() )
					return false;
			}
			while ( EndSelect() == false ); // row lock
			
			return  HasRows();
		}
		
		public bool selectIdentity ( string identity )
		{
			do
			{
				StartSelect();
				fieldSelection();
			
				m_gen_dbStatement.AddWhere ( TB_LINK_PFATIVACAO.i_unique, identity, TB_LINK_PFATIVACAO.type_i_unique );
			
				if ( !executeQuery())
					return false;
			}
			while ( EndSelect() == false ); // row lock
			
			if ( HasRows() == true)
			{
				DB_Row row = m_db_result.GetFirstRow();
				fetchRetrieve ( ref row );
				return true;
			}
			
			return false;
		}
		
		public bool create_LINK_PFAtivacao ( )
		{
			try{
			string new_id = "";
			
			StartInsert();
			
			m_gen_dbStatement.AddValue ( TB_LINK_PFATIVACAO.fk_payfone, fk_payfone, TB_LINK_PFATIVACAO.type_fk_payfone );
			m_gen_dbStatement.AddValue ( TB_LINK_PFATIVACAO.dt_ativacao, dt_ativacao, TB_LINK_PFATIVACAO.type_dt_ativacao );
			m_gen_dbStatement.AddValue ( TB_LINK_PFATIVACAO.st_ativacao, st_ativacao, TB_LINK_PFATIVACAO.type_st_ativacao );
			m_gen_dbStatement.AddValue ( TB_LINK_PFATIVACAO.tg_status, tg_status, TB_LINK_PFATIVACAO.type_tg_status );
			
			if ( !ExecuteScalar ( ref new_id ) )
				return false;
			
			i_unique = new_id;
			
			} catch ( System.Exception ex ) {
				StreamWriter wr = new StreamWriter ( 	"CRITICAL_" + var_Tablename + "_" + "_" + DateTime.Now.Year.ToString() + DateTime.Now.Month.ToString().PadLeft ( 2, '0' ) + DateTime.Now.Day.ToString().PadLeft ( 2, '0' ) + "_" + DateTime.Now.Hour.ToString().PadLeft ( 2, '0' ) + DateTime.Now.Minute.ToString().PadLeft ( 2, '0' ) + DateTime.Now.Second.ToString().PadLeft ( 2, '0' ) + ".txt" );
				wr.WriteLine ( ex.ToString() );
				wr.Flush();
				wr.Close();
			}
			
			return true;
		}
			
		public bool delete ( )
		{
		
			bool ret = false;
		
			try{
			StartDelete();
			
			if ( i_unique != "0" )
				m_gen_dbStatement.AddWhere ( TB_LINK_PFATIVACAO.i_unique, i_unique, TB_LINK_PFATIVACAO.type_i_unique );
			
			ret = Execute();
			
			} catch ( System.Exception ex ) {
				StreamWriter wr = new StreamWriter ( 	"CRITICAL_" + var_Tablename + "_" + "_" + DateTime.Now.Year.ToString() + DateTime.Now.Month.ToString().PadLeft ( 2, '0' ) + DateTime.Now.Day.ToString().PadLeft ( 2, '0' ) + "_" + DateTime.Now.Hour.ToString().PadLeft ( 2, '0' ) + DateTime.Now.Minute.ToString().PadLeft ( 2, '0' ) + DateTime.Now.Second.ToString().PadLeft ( 2, '0' ) + ".txt" );
				wr.WriteLine ( ex.ToString() );
				wr.Flush();
				wr.Close();
			}
			
			return ret; 
		}
			
		public bool select_rows_ativ ( string val_st_ativacao )
		{
			do
			{
				StartSelect();
				fieldSelection();
			
				m_gen_dbStatement.AddWhere ( TB_LINK_PFATIVACAO.st_ativacao, val_st_ativacao , TB_LINK_PFATIVACAO.type_st_ativacao);
			
				if ( !executeQuery() )
					return false;
			}
			while ( EndSelect() == false ); // row lock
			
			return HasRows();
		}
		
		#if DEBUG
		
		public bool setup ( bool IsTruncate, ref string id, string var_fk_payfone, string var_dt_ativacao, string var_st_ativacao, string var_tg_status )
		{
			if ( IsTruncate ) 
			{
				i_unique = "";
				this.delete();
			}
		
			set_fk_payfone ( var_fk_payfone );
			set_dt_ativacao ( var_dt_ativacao );
			set_st_ativacao ( var_st_ativacao );
			set_tg_status ( var_tg_status );
			
			if ( create_LINK_PFAtivacao() == false )
			{
				return false;
			}
			
			id = get_identity();
			return true;
		}
		
		#endif
		
	}
}
