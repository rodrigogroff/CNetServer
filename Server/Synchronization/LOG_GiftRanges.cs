// ###################################################### 
// ## SyCraf Engine Codegen                          #### 
// ###################################################### 
// ## This file is entirely written by               #### 
// ## the construction bot. DO NOT EDIT THIS FILE.   #### 
// ###################################################### 

using System;
using System.IO;
using System.Threading;
using System.Collections;

namespace SyCrafEngine
{
	public class LOG_GiftRanges : Synchronization
	{
		private string i_unique = "0";
		private string fk_empresa = "0";
		private string nu_inicio = "0";
		private string nu_fim = "0";
		private string dt_requisicao = "1900-01-01 00:00:00";
		
		public int var_FieldCount = 5;
		
		public LOG_GiftRanges ()
		{
			var_Serial = TB_LOG_GIFTRANGES.serial;
			var_Tablename = TB_LOG_GIFTRANGES.Alias;
		}
		
		public LOG_GiftRanges ( Transaction trx )
		{
			AcquireTransaction ( ref trx );
			
			var_Serial = TB_LOG_GIFTRANGES.serial;
			var_Tablename = TB_LOG_GIFTRANGES.Alias;
			
			TraceLog ( var_Tablename + " serial : " + var_serialNumber);
		}
		
		public string exportNames()
		{
			return "fk_empresa,nu_inicio,nu_fim,dt_requisicao";
		}
		
		public string exportCSV()
		{
			return i_unique + str_field_sep + fk_empresa + str_field_sep + nu_inicio + str_field_sep + nu_fim + str_field_sep + dt_requisicao;
		}
		
		public void Reset()
		{
			i_unique = "0";
			fk_empresa = "0";
			nu_inicio = "0";
			nu_fim = "0";
			dt_requisicao = "1900-01-01 00:00:00";
		
			ReleaseExclusive();
		}
		
		public void copy ( ref LOG_GiftRanges cpy )
		{
			fk_empresa = cpy.fk_empresa;
			nu_inicio = cpy.nu_inicio;
			nu_fim = cpy.nu_fim;
			dt_requisicao = cpy.dt_requisicao;
		}
		
		public override void fetchRetrieve ( ref DB_Row row )
		{
			i_unique = row.GetField ( TB_LOG_GIFTRANGES.i_unique );
			fk_empresa = row.GetField ( TB_LOG_GIFTRANGES.fk_empresa );
			nu_inicio = row.GetField ( TB_LOG_GIFTRANGES.nu_inicio );
			nu_fim = row.GetField ( TB_LOG_GIFTRANGES.nu_fim );
			dt_requisicao = ConvertTime ( row.GetField ( TB_LOG_GIFTRANGES.dt_requisicao ) );
		}
		
		public bool synchronize_LOG_GiftRanges ( )
		{
			try{
			
			if ( m_hshChangedFields.Count == 0 )
				return true;
			
			StartUpdate();
		
			if ( m_hshChangedFields [ "fk_empresa" ] != null )
				m_gen_dbStatement.AddUpdate ( TB_LOG_GIFTRANGES.fk_empresa , fk_empresa , TB_LOG_GIFTRANGES.type_fk_empresa );
			if ( m_hshChangedFields [ "nu_inicio" ] != null )
				m_gen_dbStatement.AddUpdate ( TB_LOG_GIFTRANGES.nu_inicio , nu_inicio , TB_LOG_GIFTRANGES.type_nu_inicio );
			if ( m_hshChangedFields [ "nu_fim" ] != null )
				m_gen_dbStatement.AddUpdate ( TB_LOG_GIFTRANGES.nu_fim , nu_fim , TB_LOG_GIFTRANGES.type_nu_fim );
			if ( m_hshChangedFields [ "dt_requisicao" ] != null )
				m_gen_dbStatement.AddUpdate ( TB_LOG_GIFTRANGES.dt_requisicao , dt_requisicao , TB_LOG_GIFTRANGES.type_dt_requisicao );
		
			m_gen_dbStatement.AddWhere ( TB_LOG_GIFTRANGES.i_unique, i_unique, TB_LOG_GIFTRANGES.type_i_unique );
		
			} catch ( System.Exception ex ) {
				StreamWriter wr = new StreamWriter ( 	"CRITICAL_" + var_Tablename + "_" + "_" + DateTime.Now.Year.ToString() + DateTime.Now.Month.ToString().PadLeft ( 2, '0' ) + DateTime.Now.Day.ToString().PadLeft ( 2, '0' ) + "_" + DateTime.Now.Hour.ToString().PadLeft ( 2, '0' ) + DateTime.Now.Minute.ToString().PadLeft ( 2, '0' ) + DateTime.Now.Second.ToString().PadLeft ( 2, '0' ) + ".txt" );
				wr.WriteLine ( ex.ToString() );
				wr.Flush();
				wr.Close();
			}
		
			return Update();
		}
			
		public string get_identity() { return i_unique; } 
		public string get_fk_empresa() { return fk_empresa; } 
		public string get_nu_inicio() { return nu_inicio; } 
		public string get_nu_fim() { return nu_fim; } 
		public string get_dt_requisicao() { return dt_requisicao; } 
			
		public long get_int_fk_empresa() { return Convert.ToInt64 ( fk_empresa ); } 
		public long get_int_nu_inicio() { return Convert.ToInt64 ( nu_inicio ); } 
		public long get_int_nu_fim() { return Convert.ToInt64 ( nu_fim ); } 
			
		public void set_fk_empresa ( string val ) { fk_empresa = val; m_hshChangedFields [ "fk_empresa" ]="."; } 
		public void set_nu_inicio ( string val ) { nu_inicio = val; m_hshChangedFields [ "nu_inicio" ]="."; } 
		public void set_nu_fim ( string val ) { nu_fim = val; m_hshChangedFields [ "nu_fim" ]="."; } 
		public void set_dt_requisicao ( string val ) { dt_requisicao = val; m_hshChangedFields [ "dt_requisicao" ]="."; } 
		
		public void set_fk_empresa ( long val ) { fk_empresa = Convert.ToString(val); m_hshChangedFields [ "fk_empresa" ]="."; } 
		public void set_nu_inicio ( long val ) { nu_inicio = Convert.ToString(val); m_hshChangedFields [ "nu_inicio" ]="."; } 
		public void set_nu_fim ( long val ) { nu_fim = Convert.ToString(val); m_hshChangedFields [ "nu_fim" ]="."; } 
			
		public void fieldSelection()
		{
			m_gen_dbStatement.AddSelect ( TB_LOG_GIFTRANGES.i_unique );
			m_gen_dbStatement.AddSelect ( TB_LOG_GIFTRANGES.fk_empresa );
			m_gen_dbStatement.AddSelect ( TB_LOG_GIFTRANGES.nu_inicio );
			m_gen_dbStatement.AddSelect ( TB_LOG_GIFTRANGES.nu_fim );
			m_gen_dbStatement.AddSelect ( TB_LOG_GIFTRANGES.dt_requisicao );
		}
			
		public bool selectAll()
		{
			do
			{
				StartSelect();
				fieldSelection();
				
				if ( !executeQuery() )
					return false;
			}
			while ( EndSelect() == false ); // row lock
			
			return  HasRows();
		}
		
		public bool selectIdentity ( string identity )
		{
			do
			{
				StartSelect();
				fieldSelection();
			
				m_gen_dbStatement.AddWhere ( TB_LOG_GIFTRANGES.i_unique, identity, TB_LOG_GIFTRANGES.type_i_unique );
			
				if ( !executeQuery())
					return false;
			}
			while ( EndSelect() == false ); // row lock
			
			if ( HasRows() == true)
			{
				DB_Row row = m_db_result.GetFirstRow();
				fetchRetrieve ( ref row );
				return true;
			}
			
			return false;
		}
		
		public bool create_LOG_GiftRanges ( )
		{
			try{
			string new_id = "";
			
			StartInsert();
			
			m_gen_dbStatement.AddValue ( TB_LOG_GIFTRANGES.fk_empresa, fk_empresa, TB_LOG_GIFTRANGES.type_fk_empresa );
			m_gen_dbStatement.AddValue ( TB_LOG_GIFTRANGES.nu_inicio, nu_inicio, TB_LOG_GIFTRANGES.type_nu_inicio );
			m_gen_dbStatement.AddValue ( TB_LOG_GIFTRANGES.nu_fim, nu_fim, TB_LOG_GIFTRANGES.type_nu_fim );
			m_gen_dbStatement.AddValue ( TB_LOG_GIFTRANGES.dt_requisicao, dt_requisicao, TB_LOG_GIFTRANGES.type_dt_requisicao );
			
			if ( !ExecuteScalar ( ref new_id ) )
				return false;
			
			i_unique = new_id;
			
			} catch ( System.Exception ex ) {
				StreamWriter wr = new StreamWriter ( 	"CRITICAL_" + var_Tablename + "_" + "_" + DateTime.Now.Year.ToString() + DateTime.Now.Month.ToString().PadLeft ( 2, '0' ) + DateTime.Now.Day.ToString().PadLeft ( 2, '0' ) + "_" + DateTime.Now.Hour.ToString().PadLeft ( 2, '0' ) + DateTime.Now.Minute.ToString().PadLeft ( 2, '0' ) + DateTime.Now.Second.ToString().PadLeft ( 2, '0' ) + ".txt" );
				wr.WriteLine ( ex.ToString() );
				wr.Flush();
				wr.Close();
			}
			
			return true;
		}
			
		public bool delete ( )
		{
		
			bool ret = false;
		
			try{
			StartDelete();
			
			if ( i_unique != "0" )
				m_gen_dbStatement.AddWhere ( TB_LOG_GIFTRANGES.i_unique, i_unique, TB_LOG_GIFTRANGES.type_i_unique );
			
			ret = Execute();
			
			} catch ( System.Exception ex ) {
				StreamWriter wr = new StreamWriter ( 	"CRITICAL_" + var_Tablename + "_" + "_" + DateTime.Now.Year.ToString() + DateTime.Now.Month.ToString().PadLeft ( 2, '0' ) + DateTime.Now.Day.ToString().PadLeft ( 2, '0' ) + "_" + DateTime.Now.Hour.ToString().PadLeft ( 2, '0' ) + DateTime.Now.Minute.ToString().PadLeft ( 2, '0' ) + DateTime.Now.Second.ToString().PadLeft ( 2, '0' ) + ".txt" );
				wr.WriteLine ( ex.ToString() );
				wr.Flush();
				wr.Close();
			}
			
			return ret; 
		}
		
		#if DEBUG
		
		public bool setup ( bool IsTruncate, ref string id, string var_fk_empresa, string var_nu_inicio, string var_nu_fim, string var_dt_requisicao )
		{
			if ( IsTruncate ) 
			{
				i_unique = "";
				this.delete();
			}
		
			set_fk_empresa ( var_fk_empresa );
			set_nu_inicio ( var_nu_inicio );
			set_nu_fim ( var_nu_fim );
			set_dt_requisicao ( var_dt_requisicao );
			
			if ( create_LOG_GiftRanges() == false )
			{
				return false;
			}
			
			id = get_identity();
			return true;
		}
		
		#endif
		
	}
}
