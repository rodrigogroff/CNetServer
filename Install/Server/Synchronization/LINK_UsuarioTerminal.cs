// ###################################################### 
// ## SyCraf Engine Codegen                          #### 
// ###################################################### 
// ## This file is entirely written by               #### 
// ## the construction bot. DO NOT EDIT THIS FILE.   #### 
// ###################################################### 

using System;
using System.IO;
using System.Threading;
using System.Collections;

namespace SyCrafEngine
{
	public class LINK_UsuarioTerminal : Synchronization
	{
		private string i_unique = "0";
		private string fk_user = "0";
		private string fk_term = "0";
		
		public int var_FieldCount = 3;
		
		public LINK_UsuarioTerminal ( infra_Patch i_patch )  : base ( i_patch )
		{
			var_Tablename = TB_LINK_USUARIOTERMINAL.Alias;
		}
		public string exportNames()
		{
			return "fk_user,fk_term";
		}
		
		public string exportCSV()
		{
			return i_unique + str_field_sep + fk_user + str_field_sep + fk_term;
		}
		
		public void Reset()
		{
			i_unique = "0";
			fk_user = "0";
			fk_term = "0";
		}
		
		public void copy ( ref LINK_UsuarioTerminal cpy )
		{
			fk_user = cpy.fk_user;
			fk_term = cpy.fk_term;
		}
		
		public override void fetchRetrieve ( ref DB_Row row )
		{
			i_unique = row.GetField ( TB_LINK_USUARIOTERMINAL.i_unique );
			fk_user = row.GetField ( TB_LINK_USUARIOTERMINAL.fk_user );
			fk_term = row.GetField ( TB_LINK_USUARIOTERMINAL.fk_term );
		}
		
		public bool synchronize_LINK_UsuarioTerminal ( )
		{
			try{
			
			if ( m_hshChangedFields.Count == 0 )
				return true;
			
			StartUpdate();
		
			if ( m_hshChangedFields [ "fk_user" ] != null )
				m_gen_dbStatement.AddUpdate ( TB_LINK_USUARIOTERMINAL.fk_user , fk_user , TB_LINK_USUARIOTERMINAL.type_fk_user );
			if ( m_hshChangedFields [ "fk_term" ] != null )
				m_gen_dbStatement.AddUpdate ( TB_LINK_USUARIOTERMINAL.fk_term , fk_term , TB_LINK_USUARIOTERMINAL.type_fk_term );
		
			m_gen_dbStatement.AddWhere ( TB_LINK_USUARIOTERMINAL.i_unique, i_unique, TB_LINK_USUARIOTERMINAL.type_i_unique );
		
			} catch ( System.Exception ex ) {
				StreamWriter wr = new StreamWriter ( 	"CRITICAL_" + var_Tablename + "_" + "_" + DateTime.Now.Year.ToString() + DateTime.Now.Month.ToString().PadLeft ( 2, '0' ) + DateTime.Now.Day.ToString().PadLeft ( 2, '0' ) + "_" + DateTime.Now.Hour.ToString().PadLeft ( 2, '0' ) + DateTime.Now.Minute.ToString().PadLeft ( 2, '0' ) + DateTime.Now.Second.ToString().PadLeft ( 2, '0' ) + ".txt" );
				wr.WriteLine ( ex.ToString() );
				wr.Flush();
				wr.Close();
			}
		
			return Update();
		}
			
		public string get_identity() { return i_unique; } 
		public string get_fk_user() { return fk_user; } 
		public string get_fk_term() { return fk_term; } 
			
		public long get_int_fk_user() { return Convert.ToInt64 ( fk_user ); } 
		public long get_int_fk_term() { return Convert.ToInt64 ( fk_term ); } 
			
		public void set_fk_user ( string val ) { fk_user = val; m_hshChangedFields [ "fk_user" ]="."; } 
		public void set_fk_term ( string val ) { fk_term = val; m_hshChangedFields [ "fk_term" ]="."; } 
		
		public void set_fk_user ( long val ) { fk_user = Convert.ToString(val); m_hshChangedFields [ "fk_user" ]="."; } 
		public void set_fk_term ( long val ) { fk_term = Convert.ToString(val); m_hshChangedFields [ "fk_term" ]="."; } 
			
		public void fieldSelection()
		{
			m_gen_dbStatement.AddSelect ( TB_LINK_USUARIOTERMINAL.i_unique );
			m_gen_dbStatement.AddSelect ( TB_LINK_USUARIOTERMINAL.fk_user );
			m_gen_dbStatement.AddSelect ( TB_LINK_USUARIOTERMINAL.fk_term );
		}
			
		public bool selectAll()
		{
			do
			{
				StartSelect();
				fieldSelection();
				
				if ( !executeQuery() )
					return false;
			}
			while ( EndSelect() == false ); // row lock
			
			return  HasRows();
		}
		
		public bool selectIdentity ( string identity )
		{
			do
			{
				StartSelect();
				fieldSelection();
			
				m_gen_dbStatement.AddWhere ( TB_LINK_USUARIOTERMINAL.i_unique, identity, TB_LINK_USUARIOTERMINAL.type_i_unique );
			
				if ( !executeQuery())
					return false;
			}
			while ( EndSelect() == false ); // row lock
			
			if ( HasRows() == true)
			{
				DB_Row row = m_db_result.GetFirstRow();
				fetchRetrieve ( ref row );
				return true;
			}
			
			return false;
		}
		
		public bool create_LINK_UsuarioTerminal ( )
		{
			try{
			string new_id = "";
			
			StartInsert();
			
			m_gen_dbStatement.AddValue ( TB_LINK_USUARIOTERMINAL.fk_user, fk_user, TB_LINK_USUARIOTERMINAL.type_fk_user );
			m_gen_dbStatement.AddValue ( TB_LINK_USUARIOTERMINAL.fk_term, fk_term, TB_LINK_USUARIOTERMINAL.type_fk_term );
			
			if ( !ExecuteScalar ( ref new_id ) )
				return false;
			
			i_unique = new_id;
			
			} catch ( System.Exception ex ) {
				StreamWriter wr = new StreamWriter ( 	"CRITICAL_" + var_Tablename + "_" + "_" + DateTime.Now.Year.ToString() + DateTime.Now.Month.ToString().PadLeft ( 2, '0' ) + DateTime.Now.Day.ToString().PadLeft ( 2, '0' ) + "_" + DateTime.Now.Hour.ToString().PadLeft ( 2, '0' ) + DateTime.Now.Minute.ToString().PadLeft ( 2, '0' ) + DateTime.Now.Second.ToString().PadLeft ( 2, '0' ) + ".txt" );
				wr.WriteLine ( ex.ToString() );
				wr.Flush();
				wr.Close();
			}
			
			return true;
		}
			
		public bool delete ( )
		{
		
			bool ret = false;
		
			try{
			StartDelete();
			
			if ( i_unique != "0" )
				m_gen_dbStatement.AddWhere ( TB_LINK_USUARIOTERMINAL.i_unique, i_unique, TB_LINK_USUARIOTERMINAL.type_i_unique );
			
			ret = Execute();
			
			} catch ( System.Exception ex ) {
				StreamWriter wr = new StreamWriter ( 	"CRITICAL_" + var_Tablename + "_" + "_" + DateTime.Now.Year.ToString() + DateTime.Now.Month.ToString().PadLeft ( 2, '0' ) + DateTime.Now.Day.ToString().PadLeft ( 2, '0' ) + "_" + DateTime.Now.Hour.ToString().PadLeft ( 2, '0' ) + DateTime.Now.Minute.ToString().PadLeft ( 2, '0' ) + DateTime.Now.Second.ToString().PadLeft ( 2, '0' ) + ".txt" );
				wr.WriteLine ( ex.ToString() );
				wr.Flush();
				wr.Close();
			}
			
			return ret; 
		}
			
		public bool select_fk_user ( string val_fk_user )
		{
			do
			{
				StartSelect();
				fieldSelection();
			
				m_gen_dbStatement.AddWhere ( TB_LINK_USUARIOTERMINAL.fk_user, val_fk_user , TB_LINK_USUARIOTERMINAL.type_fk_user);
			
				if ( !executeQuery() )
					return false;
			}
			while ( EndSelect() == false ); // row lock
			
			return HasRows();
		}
			
		public bool select_fk_term ( string val_fk_term )
		{
			do
			{
				StartSelect();
				fieldSelection();
			
				m_gen_dbStatement.AddWhere ( TB_LINK_USUARIOTERMINAL.fk_term, val_fk_term , TB_LINK_USUARIOTERMINAL.type_fk_term);
			
				if ( !executeQuery() )
					return false;
			}
			while ( EndSelect() == false ); // row lock
			
			return HasRows();
		}
	}
}
